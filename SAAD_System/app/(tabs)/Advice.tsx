import React from "react";
import { Text } from "react-native";
import AdviceView from "@/components/templates/AdviceView";
/**
 * The function `Advice` returns the `AdviceView` component in a TypeScript React application.
 * @returns The `Advice` function is returning the `AdviceView` component.
 */
export default function Advice(){
    return (
        <AdviceView />
    )
}