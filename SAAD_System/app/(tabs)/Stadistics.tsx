import React from "react";
import StadisticsView from "@/components/templates/StadisticsView";

/**
 * The function `Stadistics` returns a JSX element representing a view component for statistics in a
 * TypeScript React application.
 * @returns A JSX element `<StadisticsView />` is being returned from the `Stadistics` function.
 */
export default function Stadistics(){
    return(
        <StadisticsView />
    )
}