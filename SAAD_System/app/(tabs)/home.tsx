import React from "react";
import {Home as HomePage} from '@/components/templates/home';


/**
 * The function `Home` is a default export that renders the `HomePage` component in a TypeScript React
 * application.
 */
export default function Home(){
    return <HomePage />;
}