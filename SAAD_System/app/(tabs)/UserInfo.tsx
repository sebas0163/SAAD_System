import React from "react";
import PersonalInfoTemplate from "@/components/templates/Personal_Info";

/**
 * The `UserInfo` function is a default export that returns a `PersonalInfoTemplate` component in a
 * TypeScript React application.
 * @returns A `PersonalInfoTemplate` component is being returned.
 */
export default function UserInfo(){
    return <PersonalInfoTemplate/>;
}