import React from "react";
import ActivityTemp from "@/components/templates/Activity";

/**
 * The function `Activity` is a default export that returns the `ActivityTemp` component in a
 * TypeScript React application.
 * @returns The `Activity` function is returning the `ActivityTemp` component.
 */
export default function Activity(){
    return <ActivityTemp/>;
}